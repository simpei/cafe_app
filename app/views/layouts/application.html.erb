<!DOCTYPE html>
<html>
  <head>
    <title><%= app_title(yield(:title)) %></title>
    <%= render 'layouts/app_default'%>
    <%= render 'layouts/shim' %>
    </head>

  <body>
    <%= render 'layouts/header' %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="container">

      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
